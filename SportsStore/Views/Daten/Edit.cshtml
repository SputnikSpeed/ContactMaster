@model SportsStore.Domain.Entities.Product

<div class="panel ">
    <div class="panel-heading">

    </div>
    @using (Html.BeginForm("Edit", "Daten",
        FormMethod.Post, new { enctype = "multipart/form-data" }))
    {


        <div class="panel-body">
            @Html.HiddenFor(m => m.ProductID)
            @Html.HiddenFor(m => m.Besitzer)
            @Html.HiddenFor(m => m.ImageData)
            @Html.HiddenFor(m => m.ImageMimeType)
            @foreach (var property in ViewData.ModelMetadata.Properties)
            {
                switch (property.PropertyName)
                {
                    case "ProductID":
                    case "Besitzer":
                    case "ImageData":
                    case "ImageMimeType":
                        //do nothing
                        break;
                    default:

                        <div class="form-group container row">
                            <div class="col-xs-6 col-sm-4">

                                <label>
                                    @(property.DisplayName ?? property.DisplayName)


                                </label>
                            </div>
                                @if (property.PropertyName == "Description")
                                {       <div class="col-xs-6 col-sm-4">
                                    @Html.TextArea(property.PropertyName, null, new { @class = "form-control", rows = 5 })
                                   </div>
                             }
                                    else
                                    {    
                                        @Html.TextBox(property.PropertyName, null, new { @class = "form-control" })
                                     
                               }

                                        @Html.ValidationMessage(property.PropertyName)

                                    </div>
                                break;

                }
            }
            @*
                <div class="form-group">
                    <div style="position:relative;">
                        <label>Image</label>
                        <a class='btn' href='javascript:;'>
                            Choose File...
                            <input type="file" name="Image" size="40"
                                   style="position:absolute;
                                    z-index:2;
                                    top:0;
                                    left:0;
                                    filter: alpha(opacity=0);
                                    opacity:0;
                                    background-color: transparent;
                                    color:transparent;"
                                   onchange='$("#upload-fileinfo").html($(this).val());'>
                        </a>
                        <span class='label label-info' id="upload-file-info">
                        </span>
                    </div>
                    @if (Model.ImageData == null)
                    {

                        <div class="form-control-static">Kein Bild</div>
                    }
                    else
                    {
                        <img class="img-thumbnail" width="150" height="150"
                             src="@Url.Action("GetImage", "Product",
                            new { Model.ProductID })" />
                    }
                </div>*@


        </div>
        <div class="panel-footer">
            <input type="submit" value="Speichern" class="btn btn-primary" />
            @Html.ActionLink("Abbrechen und zurück", "List", null, new { @class = "btn btn-default" })
        </div>

    }
</div>
